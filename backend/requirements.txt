# Backend de inferencia del dashboard de dengue
# python -m pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
pyarrow>=12
scikit-learn==1.6.1      # debe coincidir con la versión que serializó los .pkl
scipy>=1.10
xgboost>=2.0
lightgbm>=4.0
torch>=2.0               # BiLSTM (CPU: --index-url https://download.pytorch.org/whl/cpu)
prophet>=1.1            # modelo nacional
